<p id="notice"><%= notice %></p>

<h1>Listing Tags</h1>

<ul>
  <% @tags.each do |tag| %>
    <li>
      <td><%= link_to tag.name, tag %></td>
    </li>
  <% end %>
</ul>

<br>

<%= link_to 'New Tag', new_tag_path %>
